markdown本文を修正した場合、releaseディレクトリの中身も更新してください。

calibreというツールとそのコマンドラインツールをインストールして、

$ bundle install
$ bundle exec rake

すればOKです。

calibreインストールするのがむずかしいとかruby環境持ってないみたいな感じならば、そのまま Pull Request ください。こちらでやります。